# Runtime dependencies for OCR Fusion Studio (framework + Streamlit app).
# Install with:  pip install -r requirements.txt
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
pillow>=10.0
pymupdf>=1.24
streamlit>=1.36
