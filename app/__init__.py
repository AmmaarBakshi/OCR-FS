"""Streamlit presentation layer for OCR Fusion Studio.

This package contains no OCR logic. It renders the framework in ``ocr_fusion``
and talks to it only through public interfaces, so the engines can change
without the UI changing.
"""
